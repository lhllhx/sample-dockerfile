spec:
  name: sample-dockerfile
  services:
  - dockerfile_path: Dockerfile
    git:
      branch: main
      repo_clone_url: https://github.com/lhllhx/sample-dockerfile.git
    name: sample-dockerfile
    envs:
    - key: FIL_WALLET_ADDRESS
      value: "DB-password-testvalue"
